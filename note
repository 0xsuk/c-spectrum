how to compile:
cd kissfft
mkdir build && cd build && cmake .. -G "MSYS Makefiles"

# go back to project root dir
mkdir build && cd build && cmake .. -G "MSYS Makefiles"
cd ..
./build/c-spectrum.exe


###########OBSOLTE (for MSVC)
DO NOT USE POWERSHELL
set PKG_CONFIG_PATH in windows to path/to/GNUWIN32/lib
make sure libpng is there
################